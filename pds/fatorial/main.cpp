#include <iostream>
#include "fatorial.h"

using namespace std;

int main(){

  cout << fatorial(5) << endl;
  cout << fatorial(0) << endl;
  cout << fatorial(1) << endl;
  cout << fatorial(2) << endl;

  return 0;
}